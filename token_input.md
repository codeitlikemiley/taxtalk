
Our main chat interface has two mode
tokenization mode and AI / normal mode.

if the user types on the first few words any valid command 

e.g. create invoice 

what would happen is 

one the input field would be cleared 

the create invoice would be so to speak be tonenized 

and be put above the input field with a tag ui 

the tag ui has an x button that you can click to remove it

the command token tag is special as if you remove it all other resolve token tag would be remove

only after we have the command token tag would it enable the tokenization mode

which is all the resolve token the user either type on input box or those he field up on the combo box after using the @ token to launch that modal , to either choose single or multiple entity

the use of @ is useful as it correctly provide the valid data from the system to our prompt

once all the required tokens are field up thats the only time we can submit the tokens to the server

we should show a green circle indicator that its complete

some token not provided which has system default would be resolve on the server

while optional token , has no effect on if you can submit or not the form only the required token

now to submit the form you can press cmd + enter or alt + enter on windows


